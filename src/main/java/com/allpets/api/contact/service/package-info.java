/**
 * Contact-form service layer: {@code ContactService} owns the transaction
 * (validate → persist → trigger staff-notification email). Added by 20.3 (email
 * trigger wired in Epic 13).
 */
package com.allpets.api.contact.service;
